unit PutChar;
# ---------------------------- PutChar.nb ----------------------------------
# --- Writing a character to the stdout
# --------------------------------------------------------------------------
do
    char x;
    
    x = "a";
    put (x);
done PutChar;